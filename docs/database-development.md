# 数据库开发约定

## 当前目标
为 Web 迁移统一数据库开发环境，避免多人各自使用不同数据库行为。

## 推荐方案
- 开发环境：PostgreSQL
- 测试环境：独立测试库
- SQLite：仅可用于临时验证，不作为正式迁移目标

## 约定
- 开发库与测试库必须隔离
- 不允许直接使用生产备份作为默认开发初始化数据
- 后续通过 Alembic 管理 schema 变更
- 编码、时区、排序规则需统一

## 当前待确认
- PostgreSQL 在本地的统一启动方式
- 开发库命名规范
- 测试库命名规范
- 初始示例数据生成策略
