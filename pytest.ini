[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    gui: GUI 测试（在 WSL2 环境中跳过）
    unit: 单元测试
    integration: 集成测试
    slow: 慢速测试
